# CaloReceipt Python dependencies
#
# Recommended for the smoothest install on both macOS and Windows:
# - use 64-bit Python 3.12 in a fresh virtualenv
# - then run: python -m pip install --upgrade pip setuptools wheel
# - then run: python -m pip install -r requirements.txt
#
# Notes:
# - These pins match the working repo/venv versions currently used by this project.
# - `paddlepaddle` is the CPU package from PyPI, which is the simplest path for
#   this repo on macOS and Windows.
# - `jaclang` stays on 0.10.0 to match `jac.toml`.

# Jac / Jaseci runtime
jaseci==2.2.14
jaclang==0.10.0
jac-client==0.2.16
jac-scale==0.1.7
jac-super==0.1.1

# Environment + LLM integration
python-dotenv==1.2.1
byllm==0.4.18
litellm==1.79.3

# OCR / image pipeline
numpy==2.1.3
opencv-python-headless==4.10.0.84
Pillow==10.4.0
paddlepaddle==3.3.0
paddleocr==3.4.0
paddlex==3.4.2

# Dev / hot reload
watchdog==4.0.2
